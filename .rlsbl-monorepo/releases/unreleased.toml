[releasables.orxt]
bump = "patch"
description = "Single-package distribution and npm publish workflow."
context = "orxt is now published as a single package on PyPI instead of 16 separate packages. npm publish workflow triggers independently."
include = []
exclude = []
