[releasables.orxtra]
bump = "patch"
description = "Single-package distribution and npm publish workflow."
context = "orxtra is now published as a single package on PyPI instead of 16 separate packages. npm publish workflow triggers independently."
include = ["pypi"]
exclude = []
