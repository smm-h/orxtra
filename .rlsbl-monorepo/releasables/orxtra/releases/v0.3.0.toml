# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for orxtra@v0.3.0; include/exclude mirror v0.13.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["pypi"]
exclude = []
description = "Typed tool returns, storage abstraction, headless mode, streaming always-on, and 15 new features."
context = "Major architectural overhaul spanning 15 phases. Tool.execute returns ToolOutput[T] with semantic result types.\nStorageBackend abstraction enables workflows without PostgreSQL. Streaming is always-on with stuck detection.\nHeadless mode has deterministic fallbacks instead of silent degradation. Autonomy gating enforced at runtime.\nOne-shot ask() API for simple LLM calls. GoogleProvider for Gemini. 3 breaking changes (knowledge-module\nremoved, stream_deltas removed, autonomy_level required)."
