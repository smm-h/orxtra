# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for orxtra@v0.12.0; include/exclude mirror v0.13.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["pypi"]
exclude = []
description = "Unified delivery and notifications, live worker WebSocket endpoint, AG-UI live streaming, strictspec validation gate, and a published documentation site."
context = "This release carries the content originally cut as v0.11.0 together with the\nstrictspec validation gate and the documentation build-out.\n\nv0.11.0 was cut under the pre-CI-gate release flow and could never pass the\nmodern publish gate: CI cannot run retroactively on a stale release commit, so\nthat version had no path to publication. Rather than attempt to resurrect it,\nits content ships here as 0.12.0 under the modern flow, which pushes the branch,\nverifies CI, and only then finalizes and tags.\n\nv0.11.0 remains a sealed, never-published version: its tag and its finalized\nchangelog stay as historical record and are not reused."
