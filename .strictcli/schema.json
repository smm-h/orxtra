{
  "schema_version": 1,
  "defaults": {
    "schema_version": 1,
    "app": {
      "env_prefix": null,
      "config": false,
      "global_flags": [],
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tag_contracts": {}
    },
    "flag": {
      "short": null,
      "default": null,
      "env": null,
      "choices": null,
      "repeatable": false,
      "unique": false,
      "env_separator": null,
      "negatable": null,
      "hidden": false
    },
    "arg": {
      "type": "str",
      "required": true,
      "default": null,
      "variadic": false,
      "choices": null
    },
    "command": {
      "passthrough": false,
      "flags": [],
      "args": [],
      "tags": [],
      "constraints": [],
      "hidden": false,
      "interactive": false
    },
    "group": {
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tags": [],
      "hidden": false
    }
  },
  "project_id": "orxtra",
  "name": "orxtra",
  "version": "0.13.0",
  "help": "Autonomous multi-agent AI workflows.",
  "global_flags": [
    {
      "name": "db",
      "type": "str",
      "help": "PostgreSQL connection URL.",
      "default": ""
    }
  ],
  "commands": {
    "serve": {
      "name": "serve",
      "help": "Start the HTTP API server (MCP, A2A, AG-UI, native routes).",
      "effect": "mutating",
      "flags": [
        {
          "name": "port",
          "type": "int",
          "help": "TCP port number for the HTTP API server to listen on."
        },
        {
          "name": "host",
          "type": "str",
          "help": "Network interface address to bind the HTTP server to.",
          "default": "0.0.0.0"
        },
        {
          "name": "secrets-env",
          "type": "str",
          "help": "JSON object mapping secret names to env var names for auth.",
          "default": ""
        }
      ],
      "forwarding": {
        "reason": "absorbs app-level global flag values the handler does not name"
      }
    }
  },
  "groups": {
    "run": {
      "name": "run",
      "help": "Manage autonomous workflow run lifecycle (start, list, show, abort, pause, resume).",
      "commands": {
        "start": {
          "name": "start",
          "help": "Start a new autonomous workflow run from a TOML configuration file and a natural-language intent. Opens a pool against the trace database, verifies the schema, dispatches start_run under an operator identity, and prints the new run's UUID on stdout so later run, trace and inbox commands can address it.",
          "effect": "mutating",
          "flags": [
            {
              "name": "config",
              "type": "str",
              "help": "Filesystem path to the run configuration TOML file."
            },
            {
              "name": "intent",
              "type": "str",
              "help": "Natural-language description of what the run should accomplish."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "list": {
          "name": "list",
          "help": "List every run recorded in the trace database, newest first, with the identifying and status fields the storage layer keeps for each one. Output renders as a human-readable table, or, under --json, as the machine document a script or agent consumes directly.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "show": {
          "name": "show",
          "help": "Display the full status report the trace store holds for one run: its current state, the intent it was started with, and the accounting the storage layer keeps alongside it. Exits non-zero with a clear message when no run carries the given identifier. Renders as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to display (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "abort": {
          "name": "abort",
          "help": "Send an abort signal to a currently executing workflow run, telling the scheduler to stop dispatching further tasks for it. The signal travels through the trace store's run-control channel rather than through dispatch, so a scheduler running in another process picks it up. Confirms on stdout unless --quiet.",
          "effect": "mutating",
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to abort (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "pause": {
          "name": "pause",
          "help": "Suspend a running workflow, telling the scheduler to stop starting new tasks for it while leaving the run and its task tree intact so it can be resumed later. The signal travels through the trace store's run-control channel, so a scheduler in another process observes it. Confirms on stdout unless --quiet.",
          "effect": "mutating",
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to pause (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "resume": {
          "name": "resume",
          "help": "Restart task execution for a workflow that was previously paused, telling the scheduler to begin dispatching its ready tasks again from the state the pause left behind. The signal travels through the trace store's run-control channel, so a scheduler in another process observes it. Confirms on stdout unless --quiet.",
          "effect": "mutating",
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to resume (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "inbox": {
      "name": "inbox",
      "help": "Manage human-in-the-loop inbox items (list, show, respond, skip, reject).",
      "commands": {
        "list": {
          "name": "list",
          "help": "List the human-in-the-loop inbox items agents have raised. Narrow the listing with --run to a single workflow run and with --status to one lifecycle state such as pending, answered or skipped; omit both to see everything. Renders as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "flags": [
            {
              "name": "run",
              "type": "str",
              "help": "Filter inbox items by run ID (only show items for this run)."
            },
            {
              "name": "status",
              "type": "str",
              "help": "Filter inbox items by status (e.g. pending, answered, skipped).",
              "default": ""
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "show": {
          "name": "show",
          "help": "Display everything the store holds for one inbox item: the question an agent asked, the options it offered, the run and task it came from, and its current resolution state. Takes the item's UUID. Renders as a readable table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "item_id",
              "help": "Unique identifier of the inbox item to display (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "respond": {
          "name": "respond",
          "help": "Submit an answer to a pending inbox item, unblocking the agent that raised the question and recording your identity as the principal that resolved it. Takes the item's UUID and the answer text. Prints the updated item, honouring the global machine document under --json, so you can confirm the response was recorded.",
          "effect": "mutating",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "answer",
              "help": "The answer text to submit as a response to this item."
            },
            {
              "name": "item_id",
              "help": "Unique identifier of the inbox item to respond to (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "skip": {
          "name": "skip",
          "help": "Resolve a pending inbox item without answering it, telling the agent that raised the question to proceed without your input. Records your identity as the principal that resolved the item. Takes the item's UUID and prints the updated item as a table, or as the machine document under --json, so you can confirm the outcome.",
          "effect": "mutating",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "item_id",
              "help": "Unique identifier of the inbox item to skip (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "reject": {
          "name": "reject",
          "help": "Reject a pending inbox item when none of the options an agent offered is usable, sending back a reason instead of an answer so the agent can reformulate. Takes the item's UUID and an explanation. Prints the updated item, honouring the item as a table, or as the machine document under --json, and records you as the principal that resolved it.",
          "effect": "mutating",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "reason",
              "help": "Explanation of why the inbox item is being rejected."
            },
            {
              "name": "item_id",
              "help": "Unique identifier of the inbox item to reject (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "trace": {
      "name": "trace",
      "help": "Query trace data: events, transcripts, tasks, and notepad entries for runs.",
      "commands": {
        "events": {
          "name": "events",
          "help": "Query the append-only event log the trace store keeps for one workflow run. Narrow the result with --type to a single event type such as task_started or tool_call, and cap its size with --limit, which defaults to a hundred. Events render as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "flags": [
            {
              "name": "type",
              "type": "str",
              "help": "Filter events by type (e.g. task_started, tool_call).",
              "default": ""
            },
            {
              "name": "limit",
              "type": "int",
              "help": "Maximum number of events to return from the query.",
              "default": 100
            }
          ],
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to query events for (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "transcript": {
          "name": "transcript",
          "help": "Display the complete stored message transcript for one agent session: every message exchanged with the model, in order, as the session module persisted it. Takes the session's identifier rather than a run's. The transcript renders as readable text, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "args": [
            {
              "name": "session_id",
              "help": "Unique identifier of the session to show the transcript for."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "search": {
          "name": "search",
          "help": "Search one agent session's stored transcript for a substring, case-insensitively, and return the matching messages rather than the whole conversation. Takes the session identifier and the text to look for. Matches render as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "args": [
            {
              "name": "query",
              "help": "Case-insensitive substring to search for in the transcript."
            },
            {
              "name": "session_id",
              "help": "Unique identifier of the session whose transcript to search."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "tasks": {
          "name": "tasks",
          "help": "List every task recorded for one workflow run with its current status, its attempt count and its place in the recursive task hierarchy, so you can see which branch of the tree stalled or retried. Takes the run's UUID. Tasks render as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to list tasks for (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "notepad": {
          "name": "notepad",
          "help": "Show the append-only cross-agent notepad entries written during one workflow run -- the messages agents left for each other as the run progressed, in the order they were appended. Takes the run's UUID. Entries render as a readable table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run to show notepad entries for (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "event": {
      "name": "event",
      "help": "Fire named events to wake wait-for tasks in running workflows.",
      "commands": {
        "fire": {
          "name": "fire",
          "help": "Fire a named event into a running workflow to wake any task waiting on it. Takes the run's UUID and an event name that must match a wait-for task's declared name; attach structured data with --payload, which must parse as a JSON object. Prints the stored event's identifier unless --quiet is passed.",
          "effect": "mutating",
          "flags": [
            {
              "name": "payload",
              "type": "str",
              "help": "Optional JSON object payload to attach to the event.",
              "default": ""
            }
          ],
          "args": [
            {
              "name": "event_name",
              "help": "Name of the event to fire (must match a wait-for task's event name)."
            },
            {
              "name": "run_id",
              "help": "Unique identifier of the run to fire the event into (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "validate": {
      "name": "validate",
      "help": "Validate agent, workflow, and category configuration TOML files.",
      "commands": {
        "agent": {
          "name": "agent",
          "help": "Validate an agent definition TOML file against the agent module's strict schema, without touching the database. Prints every error it finds to stderr and exits non-zero, or prints 'valid' and exits zero when the file is clean. Suitable as a pre-commit or CI check over a directory of agent definitions.",
          "effect": "read_only",
          "args": [
            {
              "name": "path",
              "help": "Filesystem path to the agent TOML file to validate."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "workflow": {
          "name": "workflow",
          "help": "Validate a workflow definition TOML file against the strict workflow schema without touching the database, checking the task declarations it makes and the dependency structure between them. Prints every error it finds to stderr and exits non-zero, or prints 'valid' and exits zero when the file is clean.",
          "effect": "read_only",
          "args": [
            {
              "name": "path",
              "help": "Filesystem path to the workflow TOML file to validate."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "categories": {
          "name": "categories",
          "help": "Validate a model-categories TOML file against the strict categories schema without touching the database, checking the multi-provider model routing it declares. Prints every error it finds to stderr and exits non-zero, or prints 'valid' and exits zero when the file is clean.",
          "effect": "read_only",
          "args": [
            {
              "name": "path",
              "help": "Filesystem path to the categories TOML file to validate."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "config": {
      "name": "config",
      "help": "Inspect run configuration snapshots and internal pricing tables.",
      "commands": {
        "show": {
          "name": "show",
          "help": "Display the frozen configuration snapshot the trace store captured when a run was started -- the settings that run actually executed under, rather than whatever the configuration file happens to say today. Takes the run's UUID and exits non-zero when no such run exists. Renders as a table, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "object"
          },
          "args": [
            {
              "name": "run_id",
              "help": "Unique identifier of the run whose config to display (UUID format)."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "pricing": {
          "name": "pricing",
          "help": "Display orxtra's internal pricing table for every model it knows about -- the per-token costs used to denominate run budgets in USD. Needs no database connection, since the table ships inside the installed package. Renders for a human, or as the machine document under --json.",
          "effect": "read_only",
          "payload_schema": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          },
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "db": {
      "name": "db",
      "help": "Database provisioning, schema verification, and migration commands.",
      "commands": {
        "init": {
          "name": "init",
          "help": "Create every database object orxtra's modules declare -- the trace, dispatch, identity and auth schemas -- and seed the singleton system principal. Idempotent: objects that already exist are skipped and counted rather than recreated, so it is safe to re-run. Reports what it executed unless --quiet.",
          "effect": "mutating",
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "verify": {
          "name": "verify",
          "help": "Check the connected database for every schema object orxtra expects and report how many are present and how many are missing. Names each missing object and exits non-zero when any is absent, pointing at 'db init' or 'db migrate apply' as the repair; exits zero when the schema is complete.",
          "effect": "read_only",
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      },
      "groups": {
        "migrate": {
          "name": "migrate",
          "help": "Database migration commands (wraps pgdesign).",
          "commands": {
            "plan": {
              "name": "plan",
              "help": "Preview schema changes without generating files.",
              "effect": "read_only",
              "forwarding": {
                "reason": "absorbs app-level global flag values the handler does not name"
              }
            },
            "apply": {
              "name": "apply",
              "help": "Apply pending migrations to the database.",
              "effect": "mutating",
              "consequential": true,
              "forwarding": {
                "reason": "absorbs app-level global flag values the handler does not name"
              }
            },
            "status": {
              "name": "status",
              "help": "Show applied and pending migration status.",
              "effect": "read_only",
              "forwarding": {
                "reason": "absorbs app-level global flag values the handler does not name"
              }
            }
          }
        }
      }
    },
    "dispatch": {
      "name": "dispatch",
      "help": "Manage the persistent event dispatch worker process.",
      "commands": {
        "run": {
          "name": "run",
          "help": "Start the long-running dispatch worker that delivers stored events to their subscriptions and executes the action chains those subscriptions declare. Wakes on PostgreSQL LISTEN/NOTIFY and falls back to polling every --poll-interval seconds, up to --batch-size events a batch. Use --cursor to run several workers over independent positions.",
          "effect": "mutating",
          "flags": [
            {
              "name": "cursor",
              "type": "str",
              "help": "Named cursor for this worker instance (enables multiple workers).",
              "default": "main"
            },
            {
              "name": "poll-interval",
              "type": "float",
              "help": "Fallback polling interval in seconds when LISTEN/NOTIFY is idle.",
              "default": 5.0
            },
            {
              "name": "batch-size",
              "type": "int",
              "help": "Maximum number of events to process in a single polling batch.",
              "default": 100
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    },
    "worker": {
      "name": "worker",
      "help": "Manage remote worker processes that execute tool calls for the brain.",
      "commands": {
        "connect": {
          "name": "connect",
          "help": "Run a native worker process that connects to a brain over WebSocket, authenticates with --key, registers the tool capabilities it can serve, and then executes the tool calls the brain routes to it against the project root given by --root. Runs until the connection closes or the process is stopped.",
          "effect": "mutating",
          "flags": [
            {
              "name": "brain",
              "type": "str",
              "help": "WebSocket URL of the brain to connect to (e.g. ws://host:port)."
            },
            {
              "name": "root",
              "type": "str",
              "help": "Filesystem path to the project root directory for tool execution."
            },
            {
              "name": "key",
              "type": "str",
              "help": "API key used for authenticating with the brain server."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        },
        "docker": {
          "name": "docker",
          "help": "Run a worker inside a Docker container built from --image, connected to the brain at --brain and executing tool calls against the project root given by --root. Authenticates with --key exactly as the native worker does; the container is what isolates tool execution from the host filesystem.",
          "effect": "mutating",
          "flags": [
            {
              "name": "brain",
              "type": "str",
              "help": "WebSocket URL of the brain to connect to (e.g. ws://host:port)."
            },
            {
              "name": "image",
              "type": "str",
              "help": "Docker image name to use for the worker container."
            },
            {
              "name": "root",
              "type": "str",
              "help": "Filesystem path to the project root directory for tool execution."
            },
            {
              "name": "key",
              "type": "str",
              "help": "API key used for authenticating with the brain server."
            }
          ],
          "forwarding": {
            "reason": "absorbs app-level global flag values the handler does not name"
          }
        }
      }
    }
  }
}
