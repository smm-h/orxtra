# Minimal agent definition for orxtra.
# Load with: orxtra.agent.load_agent("examples/basic_agent.toml")

name = "basic-agent"
description = "A minimal agent that reads and writes files."
category = "default"
allow = ["read", "write", "start_task", "end_task"]

[prompt]
file = "basic_agent.md"
