# DO NOT EDIT -- generated by rlsbl monorepo sync
name: Publish Router
on:
  release:
    types:
    - published
  workflow_dispatch:
jobs:
  no-op:
    runs-on: ubuntu-latest
    if: 'true'
    permissions: {}
    steps:
    - run: echo 'No publish target for this release tag'
  protocols-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: protocols/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'protocols@v')
    defaults:
      run:
        working-directory: protocols
  secrets-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: secrets/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'secrets@v')
    defaults:
      run:
        working-directory: secrets
  write-safety-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: write-safety/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'write-safety@v')
    defaults:
      run:
        working-directory: write-safety
  transport-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: transport/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'transport@v')
    defaults:
      run:
        working-directory: transport
  agent-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: agent/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'agent@v')
    defaults:
      run:
        working-directory: agent
  tool-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: tool/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'tool@v')
    defaults:
      run:
        working-directory: tool
  verify-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: verify/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'verify@v')
    defaults:
      run:
        working-directory: verify
  trace-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: trace/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'trace@v')
    defaults:
      run:
        working-directory: trace
  notepad-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: notepad/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'notepad@v')
    defaults:
      run:
        working-directory: notepad
  session-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: session/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'session@v')
    defaults:
      run:
        working-directory: session
  scheduler-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: scheduler/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'scheduler@v')
    defaults:
      run:
        working-directory: scheduler
  overseer-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: overseer/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'overseer@v')
    defaults:
      run:
        working-directory: overseer
  knowledge-module-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: knowledge-module/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'knowledge-module@v')
    defaults:
      run:
        working-directory: knowledge-module
  services-publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv build
    - uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true
        packages-dir: services/dist/
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.event.release.tag_name, 'services@v')
    defaults:
      run:
        working-directory: services
