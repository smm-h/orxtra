[project]
name = "scheduler"
version = "0.0.0"
description = "Task executor. Recursive task hierarchy, dependency graphs, budgets, constraints, crash recovery."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2",
    "jsonschema",
    "orxt-protocols",
    "orxt-write-safety",
    "agent",
    "tool",
    "session",
    "trace",
    "verify",
    "notepad",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orxt"]

[tool.uv.sources]
orxt-protocols = { path = "../protocols", editable = true }
orxt-write-safety = { path = "../write-safety", editable = true }
agent = { path = "../agent", editable = true }
tool = { path = "../tool", editable = true }
session = { path = "../session", editable = true }
trace = { path = "../trace", editable = true }
verify = { path = "../verify", editable = true }
notepad = { path = "../notepad", editable = true }
