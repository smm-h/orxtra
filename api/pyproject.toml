[project]
name = "orxtra-api"
version = "0.10.1"
license = "BUSL-1.1"
description = "HTTP compositor: mounts MCP, A2A, AG-UI, and native routes on a single ASGI app."
requires-python = ">=3.12"
dependencies = [
    "asyncpg",
    "fastware>=0.5.0",
    "strictcli",
    "orxtra-a2a",
    "orxtra-agui",
    "orxtra-auth",
    "orxtra-dispatch",
    "orxtra-identity",
    "orxtra-incoming",
    "orxtra-mcp",
    "orxtra-protocols",
    "orxtra-secrets",
    "orxtra-services",
]
keywords = ["rlsbl"]

[dependency-groups]
dev = ["pytest-asyncio>=1.4.0"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--import-mode=importlib"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orxtra"]

[tool.uv.sources]
orxtra-a2a = { workspace = true }
orxtra-agui = { workspace = true }
orxtra-auth = { workspace = true }
orxtra-dispatch = { workspace = true }
orxtra-identity = { workspace = true }
orxtra-incoming = { workspace = true }
orxtra-mcp = { workspace = true }
orxtra-protocols = { workspace = true }
orxtra-secrets = { workspace = true }
orxtra-services = { workspace = true }
