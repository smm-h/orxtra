# DISABLED: pgdesign validate no longer exists.
# Phase 1.2 replaces this with pgdesign codegen --check.
# See todo/implementation-plan.md item 1.2 for the replacement plan.
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#   workflow_dispatch:
#
# jobs:
#   validate:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#       - uses: astral-sh/setup-uv@v7
#       - run: uv tool install pgdesign
#       - run: pgdesign codegen --check
